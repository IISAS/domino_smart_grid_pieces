pytest==7.4.3
pytest-cov==4.1.0
numpy==1.23.5; python_version < "3.12"
numpy>=1.26,<2.0; python_version >= "3.12"
pandas==2.1.1
xgboost==1.7.6
scikit-learn==1.3.1
# tabpfn dragged in torch + ~3GB of CUDA wheels on CI runners and caused
# "No space left on device". The TabPFN piece still pins `tabpfn==7.0.1` in
# `dependencies/requirements_2.txt`, so the production image is unaffected.
# Tests that need tabpfn already skip when the module is not importable.